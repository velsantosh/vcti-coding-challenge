package com.vcti.ct.AAServices.config;

public interface AAConstants {
	static enum RoleEnum {
		ADMIN,RECRUITMENT, CANDIDATE, INTERVIEWER, GUEST
	};
	static enum questionTypeEnum {
		OBJECTIVE,SUBJECTIVE
	};
}
